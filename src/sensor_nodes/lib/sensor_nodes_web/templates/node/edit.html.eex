<h2><%= gettext "Edit Node Controller" %></h2>

<%= render "form.html", Map.put(assigns, :action, node_path(@conn, :update, @node)) %>

<span><%= link gettext("Back"), to: node_path(@conn, :index) %></span>
